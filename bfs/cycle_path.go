package bfs

/*type cycleState[M, T any] struct {
	state State[M, T]
}

func (cs *cycleState[M, T]) Code(*Context[M, T]) string {
	// Always return a unique code so we don't skip over the end of the cycle.
}

func ShortestCyclePath[M any, T State[M, T]](initState T, globalContext M) ([]T, int) {

}*/
